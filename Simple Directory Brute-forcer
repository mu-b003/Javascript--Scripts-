const axios = require('axios');
const paths = ['admin', 'config', 'backup', 'db'];

paths.forEach(async (path) => {
    try {
        const res = await axios.get(`http://target.com/${path}`);
        if (res.status === 200) console.log(`Found: /${path}`);
    } catch (e) {}
});
